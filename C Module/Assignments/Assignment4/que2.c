#include<stdio.h>

int main()
{
	int n;
	printf("Enter range: ");
	scanf("%d",&n);
	
	int i;      //number to check
	int j;      //condion to check armstrong in innermost loop
	int count;  //count of number of digits i.e 2
	int x=1;      //number to be multiplied i.e 10^2=100
	int k=1;      // to use in condion to find x in inner loop
	for(i=1;i<n;i++)
	{
		while(k<=)
		
		int rem,sum=0;temp=i;
		
		for(j=1;j>0;j=j/10)
		{
			rem=i%10;
			sum=sum+rem*x;
		}
		
		if(sum==temp)
			printf("%d",i)
	}
	
}
